import React from 'react'
import { css } from 'styled-components'

const Header = () => {
  return (
    <header css={css`
      height: 72px;
      background-color: black;
    `}>
    </header>
  )
}

export default Header
