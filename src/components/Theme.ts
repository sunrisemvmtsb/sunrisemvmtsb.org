export type Theme = {
  colors: {
    grayBorder: string,
    imageHoverOverlay: string,
    magenta: string,
  }
}

const theme: Theme = {
  colors: {
    grayBorder: 'rgba(0,0,0,0.1)',
    imageHoverOverlay: 'rgba(0,0,0,0.48)',
    magenta: '#8F0D56',
  }
}

export default theme
